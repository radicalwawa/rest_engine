# MIRAS_20260525_v5 — Clean Snapshot (Post Phase-2 Stabilization)

## 1. PROTOKOL
- Devir formatı: MIRAS (Successor Document Memo).
- Bu MIRAS, repo'nun 2026-05-25 tarihindeki gerçek state'ini kayda alır.
- Önceki MIRAS sürümleri: MIRAS_20260320_v3.md (Mart, repo'da mevcut).
- v4 olarak anılan bir doküman repo'ya hiç yazılmadı; bu nedenle planlama
  katmanı ile repo arasında drift oluştu. Bu MIRAS o boşluğu kapatır.

## 2. STACK (KESİN)
- Execution: Claude Code (terminal). Cursor kullanılmaz.
- Planning: Claude.ai (bu instance). Kod yazmaz; scope + protokol üretir.
- Repo: github.com/radicalwawa/rest_engine, branch=main.
- Operator: Radical (Warsaw, PL).

## 3. KALICI KURALLAR
- One-scope-per-commit. Bir commit yalnız bir mantıksal birime dokunur.
- JSON+SQL kombinasyonu: JSON domain truth, SQL operational state.
  (GOVERNANCE.md'nin orijinal "JSON only" kuralı bu commit'le AŞILMIŞTIR;
   Adım 3'te GOVERNANCE.md revize edilecek.)
- Validate-before-commit: python python/validate.py ve ilgili
  tests/validation_*.py PASS olmadan commit yok.
- FROZEN modules: db_manager.py, prompt_engine.py, daily_pipeline.py,
  sdm_tool.py, tui.py. SHA-256 hash'leri testlerde kilitli; içerik
  değişimi yalnız ayrı kapsam altında ve frozen hashes güncellemesiyle.
- Idempotent tests: her test kendi state'ini temizler, baseline'a döndürür.
- Today-aligned dates: hardcoded tarih yok; testler date.today() kullanır.
- Delta-based assertions: cumulative tablolarda (pipeline_log, sync_log)
  absolute count yerine snapshot+delta.

## 4. YENİ KURAL — GITHUB SYNC
- Her MIRAS handover'ının son adımı GitHub repo sync'tir.
- Sıra: MIRAS yaz → kapsam temizle → 10/10 validation → repo push → MIRAS imzalanmış sayılır.
- Bugünkü ilk uygulama: bu MIRAS dosyası + Adım 3'te yapılacak GOVERNANCE.md
  + VERSION + schema version revizyonu + 25+ untracked dosyanın scope-by-scope commit'i.

## 5. KEYWORD MODES (GOVERNANCE.md'den miras)
- feel. → kısa, az karakter, hisli yanıt
- think. → tablo, simülasyon, kıyas
- smart. → karmaşık yapısal problem önceliği
- execute. → koşulsuz uygulama
- /work → Claude Code prompt üretim modu, sohbet yok
- /break → planlama/sohbet modu, Claude Code prompt'u yok
- final. / shutdown → "SYSTEM STATUS: PASS. Shutdown acknowledged."

## 6. PHASE DURUMU

### Phase 1 — Operasyonel SQL katmanı (TAMAMLANDI, henüz tam commit edilmedi)
- SQLite backend kuruldu: rest_engine.db, 25 tablo (sqlite_sequence dahil),
  5 identity_v3, 5 track, 25 sound asset.
- Migrations: 001_v3_operational_layer.sql, 002_seed_identities_v3.py
- Modules: db_manager.py (~1700 satır, v3 + Phase 2 katmanlı), daily_pipeline.py, prompt_engine.py
- Validation: tests/validation_phase1.py → PASS (idempotent ✓ 2nd run da PASS)

### Phase 2 — Notes, ratings, MP3 archive, TUI (TAMAMLANDI)
- Migrations: 003_phase2_additions.sql, 003b_pipeline_log_rebuild.{py,sql},
  003c_phase2_beats_track_version.sql
- DB additions:
  - notes (color-FK, body, created_at; 14 row mevcut)
  - beats 5-boyutlu rating (rating_id/flow/beat/energy/replay, her biri 1-5)
  - beats.rating_note (serbest metin)
  - beats.mp3_path, beats.track_name, beats.version_num
  - pipeline_log.entity_type CHECK genişletildi ('notes' eklendi)
- Modules: register_beat_mp3, get_beat_versions, add_note, get_notes,
  rate_beat, get_recent_beats, _normalize_track_name, _validate_color_p2
- TUI: tui.py (RestEngineApp, active_color reactive attr, IDENTITY_THEMES,
  5 surname/identity, 3 modal: RegisterMp3Modal, RateModal, RateVersionPickerModal)
- SDM: sdm_tool.py (touch / event / mission alt komutları, positional sdm.json arg)
- Validation: 10 ayrı tests/validation_phase2_step_*.py (a, a2, b, b2, c, d, e, f, g) hepsi PASS
- Inbox workflow: C:\Users\cettb\OneDrive\Masaüstü\rest_inbox
  → archive/beats/<color>/<track>/<track>_vN.mp3

### Phase 3 — Rating-aware prompt evolution (TODO, M6 in sdm.json)
- Hedef: 5 renk için pattern extraction; rating verisi → prompt_engine'e geri besleme.
- Workflow (Radical'ın tasarımı):
  1. Suno'da şarkı üret (yazılı acapella + prompt'tan)
  2. mp3 indir → cmd'ye sürükle → register_beat_mp3 çağrılır
  3. TUI üzerinden 5 boyut (id, flow, beat, energy, replay) için 1-5 puan + rating_note
  4. notes serbest metin (color-based feedback, prompt_engine her üretimde okur)
  5. Sistem her yeni şarkıda mevcut rating + note tarihçesini prompt'a dahil eder

## 7. SCHEMA SOURCES
- domain/album_5_manifest.json — 5 state, release_format (radio + extended)
- knowledge/sound_library.json — 25 sound asset, schema_version'lı
- schemas/*.json — 5 schema dosyası, $id var ama "version" alanı YOK (Adım 3'te eklenecek)
- domain/identities_v3_seed.json — 5 renk identity seed (henüz commit edilmedi, Adım 3)

## 8. BUGÜNKÜ SESSION (2026-05-25) — ADIM 1 CHANGELOG
Sıralı commit'ler (eski → yeni):
- 7c36e8d  chore: remove stray files from repo root
- 71588d0  tests/phase2_step_c: align with renamed TUI attribute active_color
- 5fe5ac7  tests/phase2_step_b: align abort tests with real exception contract
- 02be50b  tests/phase2_step_a: rebaseline row counts to post-phase2 frozen state
- 0dd2928  tests/phase2_step_a2: rebaseline counts + sync db_manager hash to live
- e37d61d  sdm: capture phase1 + phase2 step_a..f progress (M4 to M6, M5 queued done)
- 23f83d8  sdm: touch updated_at to current session UTC
- 9f2850b  tests/phase2_step_f: delta-based assertions + per-step inbox fixture guards
- 92f79be  tests/validation_phase1: today-aligned + idempotent teardown + delta baselines
- 6da6e7a  sdm: touch updated_at before final 10/10 validation gate

Test sonuç: python/validate.py PASS + 10/10 test PASS + phase1 idempotent (2nd run PASS).

DB baseline counts (Adım 1 sonrası):
- beats          0
- notes         14
- identities_v3  5
- daily_queue    5  (queue_date='2026-05-15' — eski leftover, zararsız)
- suno_prompts   5
- pipeline_log  39
- tracks_v3      0

SDM state:
- mission.current: M6 — Phase 3 rating-aware prompt evolution (scope TBD)
- mission.queue:   [('M5', 'done')]
- contract.fingerprint: 47e605621522490758bcb1781b2ba503a332017dd652b0c5ef8b006e15679407

## 9. ADIM 3 GÖREVLERİ (BU MIRAS COMMIT'INDEN SONRA)
- Untracked dosyaları scope-by-scope commit et:
  - migrations/        (Phase 1+2 tüm SQL ve seed scripts)
  - db_manager.py + requirements.txt
  - prompt_engine.py
  - daily_pipeline.py
  - tui.py
  - sdm_tool.py + sdm.json güncellemeleri
  - scripts/close_m5_open_m6.py
  - tests/ (10 validation dosyası)
  - domain/identities_v3_seed.json
  - archive/ (3 snapshot, history-keep)
  - rest_engine.db (tracked tutmak istenirse — yoksa .gitignore'a ekle)
  - rest_sdm_masterprompt.txt
  - MIRAS_20260320_v3.md (eski)
- 3 stale remote branch'i incele (claude/phonetic-engine, setup-terminal, validate-rest-engine)
  → yararlı bir şey varsa merge, yoksa sil
- VERSION: v1.1.0 → v2.0.0 (SQL backend major change)
- schemas/*.json: her birine "version": "1.0.0" alanı ekle
- GOVERNANCE.md: "JSON is the single source of truth" cümlesini revize et —
  "JSON for domain truth, SQL for operational state, Python is tooling only"
  (no_ui_tui kuralı sdm.json'da zaten false, GOVERNANCE.md'de da yansıtılmalı)
- README.md / README_TR.md: SQL backend + TUI workflow notu

## 10. ADIM 4 — DOĞRULAMA (ADIM 3 SONRASI)
- python python/validate.py PASS
- 10/10 test PASS
- git status clean
- VERSION == HEAD uyumlu
- Origin'e push, GitHub'da repo görünür

## 11. DEVİR İMZASI
- Bu MIRAS, planning Claude tarafından yazıldı, Claude Code tarafından commit'lendi.
- Onaylayan: Radical (operator).
- Sonraki MIRAS'a kadar bağlayıcı.
