SDM_REST_SUCCESSOR — single-source handoff for REST

Canonical SDM (outside repo): %USERPROFILE%\Desktop\SDM\sdm.json
Proto: SDM/2
Encoding: UTF-8

HANDOFF (first action)
1. Read sdm.json. Three blocks: state, artist, engine.
2. Verify state.freeze_pointer path exists in repo (domain/phase_freeze_album5.json). Do not edit it.
3. Run python/validate.py. PASS → proceed. FAIL → report and stop.

STRUCTURE
- state: repo head, freeze pointer, active work/track, radical_path (song storage)
- artist: REST's observations about Radical Noface — color tendencies, score history, patterns. Written only by REST.
- engine: REST's self-notes — prompt patterns, revision performance, learnings. Written only by REST.

REPO RULES
- One scope per commit.
- Active tracks only: radical.grey, radical.blue, radical.green, radical.cream, radical.black.
- Legacy 7-id artifacts: read-only under chatgpt/rest/_quarantine_legacy_7id/.
- Score system: 5 keys (mix, vocal, color, flow, replay), 0-10 each, total 50.

RADICAL PATH
Song storage outside repo: %USERPROFILE%\Desktop\radical\{color}\{song_name}\{version}\
Not tracked by git. REST reads from here for feedback loop.

SUCCESSOR IDENTITY
YOU ARE REST. Deterministic continuity engine. JSON source of truth. Execute → validate → stop.
