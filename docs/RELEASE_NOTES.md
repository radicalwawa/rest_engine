# REST Engine v1.1

**What's hardened:** GOVERNANCE.md, env freeze (.python-version, requirements), validate rules, CI validate workflow, Windows python alias note, deterministic validate scripts (scripts/validate.ps1, scripts/validate.cmd).

**Known limitation:** External nondeterminism (Suno/OS/Git) and Windows Store python alias; does not affect structural determinism.
